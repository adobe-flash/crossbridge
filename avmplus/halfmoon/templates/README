
This directory contains the python code for defining the IR instructions,
both primitives and "fat" templates.

  hrdefs.py		 - actual definitions of primitive opcodes
                   aka "PROTOTYPES" and templates defined in terms of
				   other instructions (primitive or templates).
  ast.py		 - utilities for analyzing the templates
  sexp.py		 - s-expression parser
  templates.py	 - generates all the files in ../generated 
