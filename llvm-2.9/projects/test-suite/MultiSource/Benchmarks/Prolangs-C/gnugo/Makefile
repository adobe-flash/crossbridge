LEVEL = ../../../..

PROG = gnugo
HASH_PROGRAM_OUTPUT = 1
include $(LEVEL)/MultiSource/Makefile.multisrc

# This runs through a completely randomly-generated input
# to play a game of GO against the computer player.
STDIN_FILENAME = $(SourceDir)/stdin
