package  {		import flash.display.Sprite;	import performancetests.*;	import Dashboard;		public class PerformanceTestDashboard extends Sprite {			// Constants:			// Public Properties:			// Protected Properties:		protected var dashboard:Dashboard;			// Initialization:		public function PerformanceTestDashboard() {			stage.scaleMode = StageScaleMode.NO_SCALE;			stage.align = StageAlign.TOP_LEFT;			stage.addEventListener(Event.RESIZE, handleResize);						dashboard = new Dashboard();			dashboard.x = dashboard.y = 5;			addChild(dashboard);			layout();			setTestSuites();		}			// Public getter / setters:			// Public Methods:					// Protected Methods:		protected function setTestSuites():void {			var suiteClasses:Array = [Events,Functions,Literal];			var suites:Array = [];			for (var i:uint=0; i<suiteListing.length; i++) {				var classRef:Class = suiteListing[i];				suites[i] = new classRef();			}			dashboard.setTestSuites(suites);		}				protected function resize(evt:Event):void {			layout();		}				protected function layout():void {			dashboard.setSize(stage.stageWidth-10, stage.stageHeight-10);		}					}	}