package  {		import flash.display.Sprite;		public class Dashboard extends Sprite {			// Constants:			// Public Properties:			// Protected Properties:		protected var logger:BarGraphLogger;		protected var timer:Timer;			// Initialization:		public function Dashboard() {			setupPatterns();			layout();			timer = new Timer(1000,1);			timer.addEventListener(TimerEvent.TIMER,handleTimer);			timer.start();		}			// Public getter / setters:			// Public Methods:		public function setTestSuites(testSuites:Array):void {			if (this.testSuites) { throw(new Error("You can only call setTestSuites once.")); }			this.testSuites = testSuites;			start();		}			// Protected Methods:		protected function setupPatterns():void {			var l:uint = 4;			var diagonalPattern:BitmapData = new BitmapData(l,l,true,0);			for (var i:uint=0; i<l; i++) {				diagonalPattern.setPixel32(i,l-i-1,0xFF000000);			}			PatternRect.definePattern("diagonal",diagonalPattern);						var checkerboardPattern:BitmapData = new BitmapData(2,2,true,0);			checkerboardPattern.setPixel32(0,0,0xFF000000);			checkerboardPattern.setPixel32(1,1,0xFF000000);			PatternRect.definePattern("checkerboard",checkerboardPattern);		}				function handleTimer(evt:Event):void {			timer = null;			start();		}			}	}